<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:PPKicks;assembly=PPKicks"
    x:Class="PPKicks.ExerciseDetails"
    Title="Exercise Details">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:ImageResourceConverter x:Key="imageResourceCvt" />
        </ResourceDictionary>
    </ContentPage.Resources>
    
    <ScrollView>
        <StackLayout Spacing="10">
            <StackLayout.Padding>
                <OnPlatform x:TypeArguments="Thickness" iOS="5,20,5,5" Android="5,0,5,5" />
            </StackLayout.Padding>

            <Image HeightRequest="200" Aspect="AspectFit"
                   Source="{Binding ExerciseSnapshot, Converter={StaticResource imageResourceCvt}}" />

            <Label Text="Name" FontAttributes="Bold" />
            <Entry Text="{Binding Name, Mode=TwoWay}" />


            <Label Text="Favorite" FontAttributes="Bold" />
            <Switch IsToggled="{Binding IsFavorite, Mode=TwoWay}" />

        </StackLayout>
    </ScrollView>
    
</ContentPage>
